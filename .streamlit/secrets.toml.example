# Скопируйте этот файл как .streamlit/secrets.toml и вставьте ваш ключ.
# ВАЖНО: secrets.toml добавлен в .gitignore и никогда не попадёт в Git.
#
# Для Streamlit Community Cloud: не создавайте этот файл локально —
# добавьте ключ через: App settings → Secrets в интерфейсе Streamlit Cloud.

OPENAI_API_KEY = "your_openai_api_key_here"
